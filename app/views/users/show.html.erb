<p>User Profile</p>

<h1><%= @user.username %></h1>

<% @users_questions.each do |question| %>
<p><%= link_to question.title, question %> <%= link_to "Edit", edit_question_path %> <%= link_to "Delete", question_path, :method => :delete %></p>
<% end %>

<%= link_to "Submit a Question", new_question_path %>

